class Driver {
  final String name;
  final String email;
  final bool isVerified;
  final String id;
  final bool isOperating;

  Driver({required this.name, required this.email, required this.isVerified, required this.id, required this.isOperating});
}